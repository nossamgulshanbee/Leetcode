class Solution {
    public boolean isPowerOfTwo(int n) {
        for(int i=0;i<=100;i++)
        {
            double  x=Math.pow(2,i);
            //double b=x;
            if(x==n)
            {
                return true;
            }
        }
        return false;
        
    }
}
